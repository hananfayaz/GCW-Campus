<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item android:id="@+id/student_timetable_menu"
        android:title="@string/menu_timetable"
        app:showAsAction="never"/>
    <item android:id="@+id/student_view_subject_menu"
        android:title="@string/student_menu_sub"
        app:showAsAction="never"/>
    <item android:id="@+id/student_view_faculty_menu"
        android:title="@string/student_menu_faculty"
        app:showAsAction="never"/>
    <item android:id="@+id/logout_menu"
        android:title="@string/menu_logout"
        app:showAsAction="never"/>
</menu>